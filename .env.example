# 📁 .env.example
# Example configuration for DevHands – copy this to `.env` for local development
# IMPORTANT: Do NOT use this file directly, customize it first!

# === OpenAI API ===
OPENAI_API_KEY=sk-...

# === LangSmith (optional) ===
LANGCHAIN_API_KEY=
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
LANGCHAIN_PROJECT=devhands-local

# === Logging / Debugging ===
DEVHANDS_LOG_LEVEL=INFO

# === LLM Configuration ===
LLM_MODEL=gpt-4o
LLM_TEMPERATURE=0.3

# OPENAI_API_KEY       Your API key – required by langchain-openai
# LANGCHAIN_*          Optional – used if you want to enable LangSmith for tracing & evaluation
# DEVHANDS_LOG_LEVEL   Log level, e.g., DEBUG, INFO, WARNING, ERROR
# LLM_MODEL            Flexible model configuration (e.g., gpt-4, gpt-4o, gpt-3.5-turbo)
# LLM_TEMPERATURE      Controls creativity vs. determinism – default: 0.3
